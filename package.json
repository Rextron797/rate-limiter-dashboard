{
  "name": "rate-limiter-dashboard",
  "version": "1.0.0",
  "description": "Token bucket rate limiter simulation dashboard with CI/CD pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
